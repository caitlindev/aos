express = require 'express'
bodyParser = require 'body-parser'
concat = require 'concat-stream'

app = express()

app.use bodyParser.json()

app.post '/', (req, res, next) ->
  res.send("hello " + req.body.noun + " you are " + req.body.adjective)

module.exports = app

